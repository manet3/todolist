<Window x:Class="ToDoList.Client.ToDoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:ToDoList.Client.ViewModels"
        xmlns:controls="clr-namespace:ToDoList.Client.Controls"
        mc:Ignorable="d"
        Title="ToDo" 
        Height="400" Width="300" >
    <!-- ="{Binding FinishingCommand}"-->
    <Window.DataContext>
        <viewmodels:ToDoViewModel/>
    </Window.DataContext>
    <Window.Style>
        <Style TargetType="{x:Type Window}">
            <Setter Property="WindowStyle" Value="None"/>
            <Setter Property="AllowsTransparency" Value="True"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template" >
                <Setter.Value>
                    <ControlTemplate TargetType="Window">
                        <Border BorderBrush="Gray" 
                                BorderThickness="3"
                                Background="White"
                                CornerRadius="5">
                            <DockPanel>
                                <Border DockPanel.Dock="Top" CornerRadius="5" 
                                            MouseLeftButtonDown="Border_MouseLeftButtonDown" 
                                            Background="White">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Name="btClose" 
                                                    Style="{StaticResource closeButton}" 
                                                    Click="BtCloseClick"/>
                                    </StackPanel>
                                </Border>
                                <Line DockPanel.Dock="Top" 
                                          Fill="Transparent" 
                                          X2="{TemplateBinding Property=Width}" 
                                          Stroke="Gray" StrokeThickness="5"/>
                                <AdornerDecorator DockPanel.Dock="Bottom">
                                    <ContentPresenter/>
                                </AdornerDecorator>
                            </DockPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Style>
    <Grid Name="mainGrid"
          Background="White" 
          Focusable="True" 
          MouseDown="GridFocus" >
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Column="1" Margin="10,5,5,5" 
                 Text="{Binding ToDoItemText, UpdateSourceTrigger=PropertyChanged}"/>

        <Grid VerticalAlignment="Center" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Content="+" 
                    Style="{StaticResource add-removeButton}" 
                    Command="{Binding AddCommand}"/>
            
            <Button Content="-"
                    Grid.Row="1" 
                    CommandParameter="{Binding SelectedItems, ElementName=todoList}"
                    Command="{Binding RemoveCommand}">
                <Button.Style>
                    <Style BasedOn="{StaticResource add-removeButton}" TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedItems.Count, ElementName=todoList}"
                                         Value="0">
                                <Setter Property="IsEnabled" Value="false"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <controls:ErrorShowUp ErrorText ="{Binding NotificationMessage}"  
                              Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1"/>

        <ListView Name="todoList" 
                  SelectionMode="Multiple"
                  Grid.Row="2" 
                  Grid.Column="1"
                  Grid.ColumnSpan="2" 
                  Margin="0,5,0,5" 
                  ItemsSource="{Binding ToDoItems}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <controls:ToDoItemControl
                        ItemChangedCommand="{Binding DataContext.ChangeCommand, RelativeSource={RelativeSource AncestorType=Window}}"/> 
                </DataTemplate> 
            </ListView.ItemTemplate>
        </ListView>

        <controls:LoadingVisualiser Background="Transparent" 
                                    ActiveState="{Binding LoaderState}" 
                                    RestartButtonPressed="{Binding SyncRetryCommand}"
                                    Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" 
                                    VerticalAlignment="Top" 
                                    Margin="0,0,0,5"/>
    </Grid>
</Window>
